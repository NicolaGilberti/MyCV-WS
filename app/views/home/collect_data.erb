<%= link_to_if !session['data']['facebook_auth'], "Sign in with Facebook", user_facebook_omniauth_authorize_path %>
<% if session['data']['facebook_auth'] %> <b>Already logged in</b><% end %><br />

<%= link_to_if !session['data']['github_auth'], 'Sign in with Github', user_github_omniauth_authorize_path %>
<% if session['data']['github_auth'] %> <b>Already logged in</b><% end %><br />

<%= link_to_if !session['data']['linkedin_auth'], 'Sign in with Linkedin', user_linkedin_omniauth_authorize_path %>
<% if session['data']['linkedin_auth'] %> <b>Already logged in</b><% end %><br />
<br />
<%= button_to('Go to manage the Data', update_path, method: :post, class: 'btn btn-primary') if logged_complete?(session['data'])%>
